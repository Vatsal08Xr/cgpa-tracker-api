fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
scipy>=1.13.0        # optimization engine (LP/QP); API works without it via fallback
httpx>=0.27.0        # for TestClient in tests
pytest>=8.0.0
